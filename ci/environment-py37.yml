name: test_env_xgcm
dependencies:
  - python=3.7
  # - xarray # Now installed from master to catch breaking changes earlier.
  - dask
  - numpy
  - pytest
  - future
  - pip:
    - docrep
    - codecov
    - pytest-cov
    - black
